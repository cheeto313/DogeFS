//Devan Tatum

#include <iostream>
#include <fstream>
#include <string>
#include <sstream>
#include <cstlib>

#include "Disk.h"

void read(byte[] input){

}

bool validate(int blockNume){
	//block num must be within be in range 0..Disk.NUM BLOCKS-1 and 
	//buffer must be a byte array of exactly Disk.BLOCK SIZE bytes. 
	//Note that blocks must be read and written in whole-block units, 
	//not portions of a block.
}